(function ($) {
	$(function () {
    	pickmeup('input.cmlm-input-date', {
        	position : 'bottom',
        	hide_on_select : true,
        	default_date: false,
      });
	});
}(jQuery));